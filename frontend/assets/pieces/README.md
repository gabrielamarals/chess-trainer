# Piece assets

This directory contains one consistent Cburnett-style SVG chess set exported from the piece definitions bundled with `python-chess`.

The files are stored locally so rendering does not depend on fonts or external URLs.
